import { useState } from "preact/hooks"

export default function {{NAME}}() {
    const [data, setData] = useState(null)

    return (
        <div class="page">
            <h1>{{NAME}}</h1>
        </div>
    )
}
